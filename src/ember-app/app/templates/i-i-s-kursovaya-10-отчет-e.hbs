<h3 class="ui header">{{t "forms.i-i-s-kursovaya-10-отчет-e.caption"}}</h3>

<form class="ui form flexberry-vertical-form" role="form">
  {{flexberry-error error=error}}
  <div class="field">
    <div class="flexberry-edit-panel">
      {{#unless readonly}}
        <button class="ui button save-button" {{action "save"}}>
          {{t "forms.edit-form.save-button-text"}}
        </button>
        {{#unless model.isNew}}
          <button class="ui button save-del-button" {{action "delete"}}>
            {{t "forms.edit-form.delete-button-text"}}
          </button>
        {{/unless}}
      {{/unless}}
      <button class="ui button close-button" {{action "close"}}>
        {{t "forms.edit-form.close-button-text"}}
      </button>
    </div>
  </div>
  <div class="field flexberry-validationsummary-container">
    <div class="sixteen wide">
      {{flexberry-validationsummary errors=(v-get validationObject "messages")}}
    </div>
  </div>
  <div class="field">
    <label>{{t "forms.i-i-s-kursovaya-10-отчет-e.дата-caption"}}</label>
    {{flexberry-simpledatetime
      type="date"
      readonly=readonly
      value=model.дата
      class=(if (v-get validationModel "дата" "isInvalid") "error")
      data-test-i-i-s-kursovaya-10-отчет-e-дата=true
    }}
    {{flexberry-validationmessage error=(v-get validationModel "дата" "message")}}
  </div>
  <div class="field">
    <label>{{t "forms.i-i-s-kursovaya-10-отчет-e.тчПрограм-caption"}}</label>
    {{flexberry-groupedit
      componentName="тчПрограмGroupEdit"
      mainModelProjection=modelProjection
      modelProjection=modelProjection.attributes.тчПрограм
      content=model.тчПрограм
      readonly=readonly
      orderable=false
      class=(if (v-get validationModel "тчПрограм" "isInvalid") "error")
      data-test-i-i-s-kursovaya-10-отчет-e-тчПрограм=true
    }}
    {{flexberry-validationmessage error=(v-get validationModel "тчПрограм" "message")}}
  </div>
</form>
